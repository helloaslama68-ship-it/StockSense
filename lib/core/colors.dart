import 'package:flutter/material.dart';

// ----------------------------------------------------------
// APP COLORS
// Centralized color file used throughout the application.
// Helps maintain consistent UI design.
// ---------------------------------------------------------------
class AppColors {

  // -----------------------------------------------------------
  // BRAND COLORS
  // Main brand identity colors
  // ---------------------------------------------------------------

  // Primary orange color
  static const primary = Color(0xFFFF8C00);

  // Secondary soft orange color
  static const secondary = Color(0xFFFFB74D);

  // ----------------------------------------------------------------
  // GOLD THEME COLORS
  // Used for buttons, highlights, gradients, accents
  //-----------------------------------------------------------------

  // Dark gold color
  static const goldDark = Color(0xFF8B5E00);

  // Bright gold highlight color
  static const goldLight = Color(0xFFE3A018);

  // ------------------------------------------------------------
  // BACKGROUND COLORS
  // Used in splash screen and page backgrounds
  // ----------------------------------------------------------------------

  // Light cream top background
  static const backgroundTop = Color(0xFFF7F3EC);

  // Soft grey bottom background
  static const backgroundBottom = Color(0xFFEFEFEF);

  // ----------------------------------------------
  // BASIC COLORS
  // Commonly used standard colors
  // -----------------------------------------------------

  // Black color
  static const black = Colors.black;

  // White color
  static const white = Colors.white;

  // ------------------------------------------------------------
  // GREY SHADES
  // Used for borders, placeholders, subtitles
  // -----------------------------------------------------------------

  // Standard grey color
  static const grey = Colors.grey;

  // Light grey background color
  static const lightGrey = Color(0xFFE0E0E0);

  // --------------------------------------------------------
  // RED COLORS
  // Used for errors, warnings, delete actions
  // --------------------------------------------------------------

  // Dark red color
  static const Color darkRed = Color(0xFF93000A);

  // Light red background
  static Color lightRed = Color(0xFFFFDAD6);

  // -----------------------------------------------------------
  // TRANSPARENT COLOR
  // Used when no visible color is needed
  //-----------------------------------------------------------

  static const Color transparent = Colors.transparent;

  // ------------------------------------------------------------
  // GREEN COLORS
  // Used for success messages and stock indicators
  // ----------------------------------------------------------------

  // Dark green success color
  static const Color darkGreen = Color(0xFF119C1A);

  // ----------------------------------------------------------------
  // PURPLE COLOR
  // Used for analytics, premium UI, highlights
  // --------------------------------------------------------------------

  static const Color purple = Color(0xFF6C4AB6);

  // ---------------------------------------------------------
  // ORANGE COLOR
  // Bright orange accent color
  // --------------------------------------------------------

  static const Color orange = Color(0xFFFF8C42);
  // BLUE COLOR
  static const Color blue = Color(0xFF2196F3);
}